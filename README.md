# resuming-screening
A simple Streamlit web application that classifies resumes into predefined career categories using text cleaning, TF‑IDF vectorization, and a trained SVC model. Supports both .txt and .pdf resume uploads, cleans the text, extracts features, and predicts the most relevant job category.
